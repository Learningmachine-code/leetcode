pub struct {Solution};
impl Solution{
    pub fn find_median_sorted_arrays(nums1:Vec<i32>,nums2:Vec<i32>)->f64{
        let n=nums1.len();
        let m=nums2.len();
        let left=(m+n+1)/2;
        let right=(m+n+2)/2;
        return (getkth()

    }

    fn getkth(nums1:Vec<i16>,start1:i16,end1:i16,)
}
